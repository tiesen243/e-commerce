import { Category, Tag } from '@/types/product'

export const logo: string = 'https://raw.githubusercontent.com/tiesen243/albums/main/logo.png'

export const categories: string[] = Object.values(Category)
export const tags: string[] = Object.values(Tag)
